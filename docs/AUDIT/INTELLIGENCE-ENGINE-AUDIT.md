# INTELLIGENCE ENGINE AUDIT (2026-08-15, frozen)

Per engine: EXISTS / PRODUCTION CALLER / DECISION EFFECT / SCIENTIFIC
VALIDITY / CALIBRATED / FORWARD-ELIGIBLE / BREAKS / NEEDS.

**Digital Twin (snapshot)** — exists; called every tick; effect: RS
benchmark + capital uncertainty; valid as description; n/a calibration;
records forward-eligible; breaks on vendor outage (degrades honestly);
needs regime field (F-03) + per-facet freshness.

**Regime** — daily classifier exists but UNWIRED intraday (crude proxy
instead); effect: conservative-only caution; honest but thin; needs
intraday wiring post-freeze.

**ChartState** — exists; called per symbol per tick; effect: everything
downstream; hand-fixture-validated (VWAP/OR/returns in P1B tests +
monster-bar law); PIT-strong; breaks on data gaps (OR anchoring F-17).

**Relative Strength** — exists; called; effect: scanner + playbooks;
missing benchmark propagates None (proven, mutation-adjacent test);
small-N percentile guarded (≥5); peer set = scanned universe only
(declared limitation).

**Scanner** — exists; called; effect: watchlist; attention-not-alpha held
(thresholds frozen pre-outcome; the one real-tape fix was structural and
pre-birth); chatter finding F-16; pathological-market behavior: empty
watchlist legal, missing market ETF → no RS → no signals → funnel shows
zeros (verified in degradation test).

**Playbooks H-001/H-002** — exist; called on watchlist; effect: DECISION
records; internally coherent (see STRATEGY-SCIENCE-AUDIT); simultaneous
two-sided match impossible (VWAP+excess sign exclusivity); UNTESTED as
hypotheses — that is Monday's job.

**Analog Engine** — exists; called per candidate; effect: caution input
only; leakage-proof (poison + firewall mutations); DEFECT F-02
(single-symbol dominance → wrong OK → suppressed caution); UNCALIBRATED
and empty until forward memory accrues.

**ML Engine** — exists; called via bundle default only (UNTRAINED);
effect: none (correct); future path has purge/embargo/N_eff gates; DEFECT
F-05 (nan-to-zero) must be fixed before first training.

**ForecastBundle** — exists; called; effect: carries disagreement +
support to capital; conflict preserved in all six audit cases; source
status derived; no averaging path exists.

**Distribution Engine** — exists (daily estimator); Hunter path: socket
only, REFUSED status (correct); PMF/quantile laws tested in MVPM suite.

**World Simulator** — exists; NOT in hot path (on-demand, documented);
effect: none on decisions (authorization power NONE); counterfactuals
plausible (calm vs violent donors); DEFECT F-04 (fabricated ATR default);
future-blind proven.

**Swarm** — interface exists; called; effect: none (BLOCKED_EXTERNAL_
AUTH, zero claims enforceable by type); cannot reach stop/target/risk/
capital in the call graph (searched).

**Opportunity/Capital** — exists; called per candidate; effect: final
states; monotone caution proven incl. under mutation; sovereign constants
verified single-source; COST_UNKNOWN honest.

**Trade Manager** — exists; production-idle; counterexamples pass; DEFECT
F-11 (duplicate law vs sealed state machine + missing TradeThesis
construction) — dormant.

**Research Memory** — exists; ledger IS the store; every candidate kind
retained (selected/rejected/refused/baselines); BEFORE immutable
(chained); time firewall proven; DEFECT F-06 (regime 0.0 label).
