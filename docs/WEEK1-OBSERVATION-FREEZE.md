# WEEK 1 OBSERVATION FREEZE (declared Saturday 2026-08-15, operator ruling)

For at least the first forward week (sessions of 2026-08-17 through
2026-08-21), a HARD FREEZE applies to all predictive logic:

- Hunter-001/002 predicates: FROZEN. "Didn't fire much" is a finding.
- Scanner thresholds: FROZEN. "Too many/too few abnormalities" is a finding.
- Analog similarity schema/thresholds: FROZEN. Sparse support is a finding.
- Baselines, N_effective, graduation criteria: FROZEN (already law).

Permitted: engineering-defect fixes ONLY — versioned, re-birthed,
append-only, with the defect named. A defect is code not doing what its
frozen spec says; it is never the spec disliking reality.

The question this week is NOT "did we make money?" It is "is the machine
seeing the market correctly?" — answered by the health report
(scripts/hunter_week_report.py), read as findings, never as knobs.

Swarm commissioning note: when CLI auth arrives, the transport requires a
smoke test AND its own birth registration before any assessment may
attach to a candidate; earlier candidates are never retroactively
enriched. Swarm output gets a birth timestamp like everything else.
