# Funnel first-run pre-registration (written and committed BEFORE the run, 2026-09-12)

`FULL_FUNNEL_V1` has never executed. Four weeks of funnel work has produced no execution on any data with a real
variance fit, because the fit needs 400 prior-session bars and no run has ever supplied them.

**This is a MECHANISM test.** Pre-registered, before any result exists:

1. Its purpose is to answer: does the funnel produce candidates, does anything reach PRIME, what does it select,
   and does that selection differ from the rule path's.
2. **Excluded from expectancy, calibration, alpha and any claim about edge.** Whatever it selects, and whatever
   that selection would have earned, is not evidence about the strategy.
3. Data: the burned 2026-09-11 SPY session plus the seven days of prior 1-minute bars the Alpaca client serves for
   the fit. **Quarantined as replay**, written to an isolated ledger, never merged.
4. If the funnel has a defect, that is the finding and it is reported today.
5. The expected outcome is unknown. Both "it works" and "it has never worked" are reportable; neither is a surprise.
6. No parameter is tuned to make it produce candidates. If it produces none, the named reason is the result.
