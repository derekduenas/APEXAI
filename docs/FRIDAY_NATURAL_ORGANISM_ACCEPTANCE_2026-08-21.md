# APEX — FRIDAY NATURAL ORGANISM ACCEPTANCE DIRECTIVE
# Operator-issued 2026-08-21 pre-dawn. Execute during/after the
# 2026-08-21 session. MODE: OBSERVE / TRACE / FALSIFY.
# NO strategy development, parameter changes, threshold changes, or
# repairs unless P0 evidence-integrity/safety.
#
# The full 25-section directive is recorded in the session transcript
# and in ~/.claude memory (apex_equities.md tail). Key sections:
#  0. Pre-flight (suite/registry/authority/launchd/disk/clock; sealed:
#     live capital, broker, Credit 5, holdout) -> READY / NOT_READY
#  1. FORWARD_LEARNING_CAUSALITY invariant (PROVEN pre-market
#     2026-08-21: tests test_forward_learning_causality_* -- re-verify)
#  2. Outcome resolver sanity (DONE pre-market for Thursday's 60:
#     60/60 independent agreement, QUIET_MARKET_CONFIRMED, but audit
#     found BAD-PRINT bar lows poisoning 16 MAE values -> condition
#     filter added to bar_builder + 16 append-only quality flags;
#     REDO the independent reconstruction on Friday's outcomes)
#  3. Sample independence (n_raw / n_effective_lower_bound / clustering
#     now persisted -- report them; never lower gates)
#  4. Organ pulse: for every LIVE_ACTIVE organ report session call/output
#     counts + last timestamps from its ledgers; classify LIVE_ACTIVE /
#     LIVE_STALLED / DECLARED_ONLY / TERMINAL / INTERFACE_ONLY / DARK.
#     Specifically: outcome resolver, probability, information lead,
#     forecast layer, chain adapter, crypto adapter.
#  5. Sensor: FIRST session with diagnostic reconnect telemetry --
#     report every reconnect's close_code/close_msg/error_type/derived
#     reason/queue/lag; summarize actual cause categories. Do NOT
#     assume Thursday's cause. Also: frames dropped, tape loss,
#     completeness. NOTE: Friday is also the first session with the
#     sale-condition high/low filter -- verify no bad-print lows.
#  6. Curve V2 natural acceptance (no tuning on natural frequencies).
#  7. Canonical cross-sectional state consistency (no duplicate calc).
#  8. Data sufficiency refusals (feature/quality/reason/blocked).
#  9. LeadingEdge (rank calls/outputs/entry_quality distribution).
# 10. Assassin2 natural wound rates (no tuning; starved stay starved).
# 11. Captain (zero natural SERIOUS is NOT a failure; controlled
#     reachability must remain).
# 12. Forecast layer per opportunity (status + live gate counts incl.
#     n_effective; numeric authorization forecast from anything except
#     CALIBRATED = P0/P1).
# 13. Capital (must remain able to refuse SERIOUS).
# 14. Options expression: first natural chain-adapter test (chain must
#     be CURRENT; stale chain may not compete; full contract economics
#     on every candidate; every refusal named).
# 15. BEFORE cards sealed pre-outcome; forbidden fields = 0.
# 16. NO_TRADE_DECISION vs PIPELINE_NEVER_REACHED_EXPRESSION counts.
# 17. Outcome loop LIVE: newly terminal, exactly-once, corpus counts
#     changing during the session (e.g. P006 8 -> 9 -> ...).
# 18. Information lead per newly-resolvable episode; no verdict below n.
# 19. probability.estimate() genuinely called; numeric probability may
#     still be 0 -- fine.
# 20. Pattern Observatory day 2: collision-free v2 ids, support growth,
#     terminal transitions, zero historical contamination.
# 21. Crypto terminology: CRYPTO_CONTEXT LIVE / BTC_PERPS_DERIVATIVES
#     NOT_AVAILABLE; funding/OI/basis/liquidations = NOT_DERIVED.
# 22. Checkpoint funnel per natural opportunity (CP01..CP15 with
#     PASS/REFUSE/NOT_REACHED + reason); furthest natural traversal.
# 23. ONE NATURAL GOLDEN TRACE: the furthest-travelling real
#     opportunity, complete chronological trace, event_time + known_from
#     + checkpoint + decision at every step.
# 24. Red team: future outcomes in corpus / pseudo-replication /
#     same-session correlation / stale chain / nearest-bar substitution
#     / phantom LIVE_ACTIVE / duplicate market-state calc / unknown->0 /
#     bad quality raising confidence / SERIOUS implying approval /
#     unsealed cards / spot-as-perps.
# 25. EOD ruling per the operator's exact scoreboard, ending in
#     ORGANISM_ACCEPTANCE: PASS / DEGRADED / FAIL and
#     READY_FOR_EVIDENCE_EXPANSION: YES / NO.
#
# FINAL LAW: reward stopping at the CORRECT checkpoint, refusing
# unsupportable forecasts, and preserving causal truth -- never depth,
# volume, or trades. The question: can this organism experience the
# market, decide using only what it knew then, observe what happened
# next, and learn from it WITHOUT LYING TO ITSELF?
#
# IF PASS: operator authorizes Evidence Expansion v1 (multi-subject
# observatory, industry topology, liquidity sensor, regime model, OOD
# state vector, real BTC-perps feed, pre-registered new families,
# faster outcome accumulation, forecast calibration).
