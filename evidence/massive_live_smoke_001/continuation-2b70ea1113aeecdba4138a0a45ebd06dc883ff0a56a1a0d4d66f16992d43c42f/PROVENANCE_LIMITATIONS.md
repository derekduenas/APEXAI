Raw connector text and complete returned tool objects are preserved separately from normalization. JSON serialization is not provider wire bytes. Source attribution is UNVERIFIED. Receipt timestamps are current connector observation times, never historical availability. Bar completion is only an earliest availability assumption; delivery and archive revisions are unknown. Contract as_of is a request parameter; response CSV omits vendor response metadata. Pagination completeness is not proven. No NBBO means no contract trading admission and no simulated execution.

Existing preflight inventories only sessions.inputs, not the files list, and does not attest opened-file identity or detect replacement races. A separate hash inventory here checks declared files but cannot prove no historic replacement. Read-only reopening occurred for validation; no claim of never reopening is made. Prior evidence remains untouched. Bar boundary perturbation tests transport visibility only, not model downstream behavior. No fit, tuning, orders, scheduler, deployment, or profitability claim.
